using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class ClassSelectionData
{
    public static string SelectedClass = "Diver"; 
    // the default player class and where it gets called
    // so this script isnt attached to anything in game.
}